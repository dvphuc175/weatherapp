<?xml version="1.0" encoding="utf-8"?>
<!--
  Drawable used as `app:tabBackground` for the multi-city pager dots. The shape is wrapped in a
  layer-list with `gravity="center"` and an explicit `size` so the dot stays a fixed-diameter
  circle regardless of the tab's measured bounds. (Without the layer-list, TabLayout stretches
  the drawable to fill the tab, which makes the dot look like a tall pill.)
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <layer-list>
            <item android:gravity="center">
                <shape android:shape="oval">
                    <size android:width="8dp" android:height="8dp" />
                    <solid android:color="@color/text_primary_on_brand" />
                </shape>
            </item>
        </layer-list>
    </item>
    <item>
        <layer-list>
            <item android:gravity="center">
                <shape android:shape="oval">
                    <size android:width="8dp" android:height="8dp" />
                    <solid android:color="#66FFFFFF" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
